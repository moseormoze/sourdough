# Tasks: טיימרים ניתנים לשינוי בשלבי המתנה

## T1 — יישור ומימוש טיימר משותף

**Branch:** `codex/feature/27-configurable-stage-timers/T1-reconcile-stage-timers`

### Scope

1. לכתוב בדיקות נכשלות לחוזה הבורר, מטריצת השלבים, lifecycle של הבחירה, תאימות
   לנתונים ישנים ומסלול T2 של האוטוליזה.
2. להרחיב את `OptionalTimer` בבורר native ולהגדיר אפשרויות וזמני ברירת מחדל לכל
   שלב המתנה.
3. לאחד את ה-state סביב `timerDurationSeconds`, למנוע שינוי מחוץ ל-idle ולשמור
   בחירה ב-reset אך לנקות אותה במעבר שלב.
4. להסיר את רכיבי האוטוליזה הייחודיים ואת עריכת הזמן בזמן ריצה.
5. לשחזר את מסלול T2 המאושר על בסיס `main` הנוכחי וליישב את חפיפות PR #70.
6. להריץ בדיקות, type-check, lint, בדיקת RTL ובדיקה חזותית לפני פתיחת PR.

### Done when

- כל קריטריוני הקבלה ב-`brief.md` מכוסים ועוברים.
- אין שני מנגנוני timer ואין conflict markers.
- הבדיקה החזותית בעברית/RTL עברה וה-PR מקשר ל-Issue #72.
