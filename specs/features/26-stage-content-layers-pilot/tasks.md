# Tasks: פיילוט עומק וכיול — אוטוליזה

**סטטוס:** T1–T5 הושלמו; T5 מוזג ב־PR #82. T6 הושלם ואומת ומוכן לבדיקת PR
ולמיזוג משתמש. T7 מאושר וממתין למיזוג T6, בהתאם לכלל משימה אחת בכל פעם.

## T1 — יישור גרירת `BottomSheet` ל־UI Playbook

**הושלם:** PR #68. חוזה הגרירה, rubber-band, cleanup, focus ו־reduced motion נשמר.

## T2 — המסלול הראשי הבסיסי של אוטוליזה

**הושלם:** PR #69. ארבע הפעולות, הטיימר, שלושת סימני ההמשך ומשפט המעבר נשמרים
כבסיס. T3 רשאי להוסיף מדיה ביניהם, אך אינו משנה אף טקסט קיים, את התזמון או את
כמויות המתכון.

## T3 — כיול חזותי ומדריך עומק מבוסס מתכון

**Goal:** להחליף את ה־Hub, ה־FAQ והחילוץ שנדחו במסלול שמספק שני ערכים: ייחוס
חזותי במקום שבו האופה מערבב, ומדריך אופציונלי שמסביר את הטרייד־אוף ומתאים כיוון
לבצק הנוכחי.

**Branch/PR:**
`codex/feature/26-stage-content-layers-pilot/T3-align-contract` · PR #78

**מצב נוכחי שצריך להחליף:**

- `StageKnowledgeHub` בעל שלוש שורות בולטות.
- שלושה סוגי תוכן `learn`/`faq`/`troubleshooting`.
- 5 מקטעים, 6 שאלות ו־2 תרחישים שנאכפו בחוזי data ובבדיקות.
- `StageKnowledgeSheet` שבוחר layout לפי קטגוריה.
- שלב 2 ללא מדיה; `StageMedia` הכללי מרונדר לפני `InstructionCard`, ולכן אינו
  מתאים למיקום החדש בלי לשנות את יתר השלבים.

### Deliverables

1. **חוזה תוכן חדש**
   - מודל מדריך יחיד שמכיל מנגנון, טרייד־אוף, גרף, התאמת מתכון וחריגים אמיתיים.
   - פונקציה טהורה שמקבלת `recipe.flour`, ‏`recipe.hydration` ו־
     `recipe.kitchenTemp` ומחזירה לכל היותר שלושה גורמים איכותניים וסיכום כיוון.
   - אין חישוב דקות חדש, מכסת FAQ או תרחיש ללא סימן ופעולה אמינים.
   - המודל מקבל רק את הקופי שסופק ואושר על־ידי המשתמש ב־Design.

2. **מדיית כיול ייעודית לאוטוליזה**
   - רכיב שלב־2 שמרונדר אחרי `InstructionCard` ולפני `AutolyseTimer`.
   - מציג שני מצבים של אותו בצק, כיתובים נגישים ו־fallback אמיתי/טקסטואלי.
   - `StageMedia` והסדר שלו בשלבים 1 ו־3–12 אינם משתנים.

3. **כניסת עומק שקטה אחת**
   - טריגר יחיד אחרי `ChecklistReference`, ללא card shadow, אייקונים גדולים או
     מוני תוכן.
   - state machine: `Idle → Press → Open|Cancel → Idle`; תנועה מעל 5px אינה
     פותחת והפעלת מקלדת נשמרת.

4. **מדריך עומק גליל אחד**
   - `BottomSheet` מלא אחד ללא overlays מקוננים.
   - גרף מושגי נגיש עם חלופת טקסט שאינו מציג ערכים לא מכוילים.
   - בלוק הקשר למתכון שמציג את ההכוונה האיכותנית מהחוזה הטהור.
   - פתיחה/סגירה שומרות את מסך הבייק, הטיימר והגלילה ומחזירות focus לטריגר.

5. **הסרת הכיוון שנדחה**
   - למחוק או להחליף את רכיבי ה־Hub/Sheet, המודל, המחרוזות והבדיקות שאוכפים
     `learn`/`faq`/`troubleshooting`.
   - לעדכן את תיאור PR #78 כך שיתאר רק התנהגות שנשארה במוצר.

### Files likely touched

- `lib/data/stage-knowledge.ts` והבדיקות — החלפה במודל מדריך/הכוונה ממוקד.
- `components/bake/stage-knowledge-hub.tsx` והבדיקות — החלפה בטריגר יחיד.
- `components/bake/stage-knowledge-sheet.tsx` והבדיקות — החלפה במדריך יחיד.
- רכיב חדש או קיים למדיית כיול של אוטוליזה + בדיקות.
- `components/bake/stage-screen.tsx` והבדיקות — מיקום המדיה וחיבור נתוני המתכון.
- `lib/strings.ts` והבדיקות.
- רכיב ההטמעה מתעד את מזהה הסרטון; אין קובץ מדיה תחת `public/`.

### Test-first contract

לפני שינוי המימוש, לעדכן בדיקות כך שייכשלו מול הקוד הנוכחי ויוכיחו:

- שלב 2 מציג מדיית כיול בין ההוראות לטיימר; יתר השלבים שומרים את הסדר הקיים.
- אין Hub של שלוש שורות ואין labels/מונים של FAQ וחילוץ.
- טריגר עומק יחיד נגיש ונפתח פעם אחת ב־tap או מקלדת, ולא אחרי scroll/cancel.
- המדריך מציג את ארבע שכבות הערך: מנגנון, טרייד־אוף/גרף, התאמת מתכון וחריגים.
- כללי ההכוונה מחזירים תוצאה דטרמיניסטית עבור תערובות/הידרציות/טמפרטורות
  מייצגות, ומצב כללי כשנתון חסר; הם אינם מחזירים זמן מדויק.
- כל מחרוזות המסלול הראשי של שלב 2 זהות למצב שלפני T3, והקופי החדש תואם מילה
  במילה לקופי שיספק המשתמש.
- הגרף כולל כותרת וחלופת טקסט ואינו תלוי בצבע להבנת המסר.
- פתיחה וסגירה שומרות `activeBake`, ‏`currentStage` והטיימר ומחזירות focus.
- טווחים ויחידות נשמרים LTR, וכל התוכן בעברית/RTL.

### Verification

- בדיקות ממוקדות עוברות לפני הרצת הסוויטה המלאה.
- `npm test`, ‏`npm run type-check`, ‏`npm run lint` ו־`rtl-check` עוברים.
- QA ב־375px בודק את המסך, שני מצבי המדיה, הגרף, תוכן ארוך, כשל מדיה,
  מקלדת/reduced motion והיעדר חפיפה עם הפעולות הדביקות.
- ה־Preview נבדק חזותית; בדיקת מקור בלבד אינה מספיקה.
- ביקורת מוצר מאשרת שההעמקה משנית ושכל מקטע מוסיף החלטה או הבנה.

### Done when

- [x] תוכנית המדיה וכללי ההכוונה אושרו וננעלו ב־Design.
- [x] קופי עבודה סופק על־ידי המשתמש לפני מימוש ה־UI התלוי בו.
- [x] הבדיקות החדשות נכשלו מול המימוש שנדחה לפני החלפתו.
- [x] fallback מדיית הכיול והמדריך החדש ממומשים; הכיוון הישן הוסר.
- [x] ה־Short המאושר נגיש דרך עמוד YouTube המקורי ללא הורדה/אחסון מחדש,
  והכיתובים נשארים גלויים כ־fallback.
- [x] כל הווריפיקציות הטכניות ו־QA חזותי מקומי ב־375px עוברים.
- [ ] קופי מדריך העומק מדויק ומאושר סופית על־ידי המשתמש.
- [ ] ה־Preview שפורסם עובר QA חזותי וביקורת משתמש.
- [ ] PR #78 מעודכן, מוכן לביקורת ואינו מתואר כמוכן למיזוג לפני ביקורת המשתמש.
- [ ] הפיילוט נבדק בבייק אמיתי לפני החלטה על הרחבה.

## Blockers before Ready

- T4: QA חזותי ב־Preview וביקורת משתמש.
- אחרי T4: דיוק קופי מדריך העומק וביקורת בבייק אמיתי לפני הרחבה.

## T4 — Redesign מלא של עמוד האוטוליזה

**Goal:** להפוך את שלב 2 ממסמך כרטיסים קבוע למסך מכוון־רגע שמבהיר ביצוע, אימות,
המתנה, חזרה והחלטה; להוכיח שפה חזותית שניתן לבחון לפני הרחבה למוצר.

**Branch:** `codex/feature/26-stage-content-layers-pilot/T4-redesign-pilot`

### Deliverables

1. **מבנה עמוד ייעודי לשלב 2**
   - hero מאוחד ל־header ול־briefing.
   - משטח ביצוע אחד עם ארבע ההוראות הקיימות.
   - הפרדה בין אימות הערבוב לבין סימני סוף המנוחה בלי לשנות קופי.
   - variants ייעודיים לפיילוט; שלבים אחרים אינם משתנים.

2. **מדיית כיול קומפקטית**
   - אימות הערבוב ושני כיתובי הכיול גלויים במסלול הראשי.
   - הקישור הוא block surface יציב גם כשהטקסט נשבר למספר שורות או מוגדל.
   - אין iframe: הקישור פותח את `youtube.com/shorts/0JzkxDMnDhI` בלשונית חדשה.
   - עמוד הבייק נשאר פתוח והשלב עובד גם כשהיעד החיצוני אינו זמין.

3. **היררכיית טיימר וחזרה**
   - idle, running, paused ו־finished מקבלים `data-state` ונראות מובחנת.
   - כרטיס קומפקטי בעמוד מציג סטטוס, זמן, קו ספקטרום ופעולות ישירות; אין טבעת זמן.
   - `BottomSheet` משמש בחירה/עריכה בלבד; אין מסך ספירה מורחב או חיווי ב־hero.
   - מצב מחושב מה־timestamp והזמן המצטבר השמורים ואינו מתאפס בחזרה לעמוד.

4. **פעולת המשך מכוונת־מצב**
   - לפני finished ״הבא״ משני ופותח אישור קל, ללא חסימה.
   - אחרי finished ״הבא״ מתקדם ישירות אך נשאר משני; הטיימר אינו מאמת מוכנות.
   - האישור מציג מצב וזמן קיימים ואינו מציג את משפט המעבר לפני finished.
   - אין מעבר אוטומטי או קופי חדש; פעולת הביטול ממחזרת `strings.common.cancel`.

5. **שפה חזותית של הפיילוט**
   - light-first עם peach רווי מקומית, cool-blue משני, whitespace ורדיוסים גדולים.
   - פחות משטחים מתחרים, ניגודיות AA, RTL, mixed-direction numbers ו־44px.
   - הגרף המושגי נשאר סכמטי, ללא מילוי/הילה/נקודות קצה שמרמזים על מדידה.
   - ניגודיות CTA עומדת ב־AA וה־FAB אינו חופף למסכי הבייק הפעילים.
   - אין שינוי גלובלי בשלבים אחרים לפני ביקורת הפיילוט.
   - מדריך האוטוליזה כולו משתמש ב־variant של הפיילוט ובחמש שכבות מובחנות; הקופי
     הקיים נשמר ללא שינוי.

### Files likely touched

- `components/bake/stage-screen.tsx` והבדיקות.
- `components/bake/stage-header.tsx`, `briefing.tsx`, `instruction-card.tsx`,
  `checklist-reference.tsx` — variants לפיילוט בלבד.
- `components/bake/autolyse-calibration.tsx` והבדיקות.
- `components/bake/autolyse-timer.tsx` והבדיקות.
- `app/globals.css` רק אם נדרש utility ממוקד; אין שינוי טוקנים גלובלי בלי צורך.

### Test-first contract

- stage 2 מציג root של הפיילוט והשלבים האחרים שומרים את ה־DOM וה־variants הקיימים.
- ״אין כיסי קמח יבש״ נמצא באזור אימות הערבוב; שני סימני סוף המנוחה נשארים באזור
  ההחלטה והקופי המלא זהה ל־T2.
- אין iframe; קישור המדיה מפנה ל־Short המקורי בלשונית חדשה ושומר את עמוד הבייק.
- idle/running/paused/finished מחושבים דטרמיניסטית, כולל timestamp ישן המדמה
  חזרה לאפליקציה.
- לפני finished לחיצה על ״הבא״ אינה מתקדמת לפני confirm; אחרי finished היא
  מתקדמת פעם אחת בלי dialog.
- כל פעולה היא 44px לפחות, עובדת במקלדת ושומרת focus/reduced motion.

### Verification

- בדיקות T4 נכתבות ונכשלות לפני המימוש.
- בדיקות ממוקדות, `npm test`, `npm run type-check`, `npm run lint` ו־`rtl-check`.
- QA מרונדר ב־375px למצבי idle/running/paused/finished, קישור המדיה, טקסט מוגדל
  ויעד מדיה לא זמין; אין overflow או חפיפה עם הפעולות הדביקות/feedback.
- ה־PR כולל את יישור המסמכים והמימוש יחד ומוגש לביקורת משתמש אחת.

### Done when

- [x] חוזי T4 כתובים ונכשלו מול baseline של PR #78.
- [x] כל deliverables ממומשים בלי לשנות קופי קיים או שלבים אחרים.
- [x] כל הווריפיקציות הטכניות והחזותיות עוברות.
- [x] Night Run: שלושה אודיטי מומחים, 977 בדיקות, type-check, lint, RTL,
  production build ו־QA ב־375px/320px עברו.
- [x] תיקוני ביקורת 2026-08-03: כרטיס טיימר בעמוד, Sheet לעריכה בלבד, hero ללא
  חיווי כפול, כתום מקומי חזק ועיצוב מלא למדריך עברו QA מרונדר ב־375px.
- [x] תיקון ממוקד 1: כרטיס הטיימר הקומפקטי הוחזר וטבעת הזמן הוסרה, ללא שינוי
  נוסף במסך.
- [ ] Preview זמין והמשתמש מאשר את הפיילוט או מחזיר תיקוני UI ממוקדים.

### Copy decisions for user review

- הסבר מפורש ב־Sheet של מעבר מוקדם.
- כלל החלטה קצר במסלול הראשי כשהשינוי בבצק עדין.
- ריכוך כיתוב הכיול והבהרה שהשעה בסרטון אינה המלצה לבייק הנוכחי.
- שם נגיש לקישור שמבהיר כי הוא פותח וידאו חיצוני.

## T5 — תקינות מצב ומחזור חיים של הטיימר

**Goal:** עריכת הזמן משמרת את מצב הריצה הקיים, למסך האוטוליזה יש מקור tick אחד,
וכל interval מפסיק לעבוד כשהטיימר מגיע ל־`finished`.

**Branch:** `codex/feature/26-stage-content-layers-pilot/T5-timer-state-lifecycle`

### Scope

- `setTimerRemaining` משנה את הזמן שנותר בלי להפוך טיימר מושהה ל־`idle`.
- טיימר פעיל ממשיך לרוץ לאחר עריכה; טיימר מושהה נשאר מושהה.
- `StageScreen` הוא בעל השעון היחיד של כרטיס האוטוליזה ומעביר את הזמן המחושב
  ל־`AutolyseTimer`; אין interval כפול.
- tick נעצר ב־`finished` גם בטיימר האוטוליזה וגם ב־`OptionalTimer` שמשתמש באותו
  דפוס lifecycle.
- אין שינוי בעיצוב, בקופי, במשך ברירת המחדל, ב־localStorage או בהתנהגות ״הבא״.

### Files likely touched

- `lib/hooks/use-active-bake.ts` והבדיקות.
- `components/bake/stage-screen.tsx`, `autolyse-timer.tsx` והבדיקות.
- `components/bake/optional-timer.tsx` והבדיקות.

### Test-first contract

- עריכת טיימר מושהה שומרת `timerStartedAt: null`, את הזמן שכבר חלף ואת זמן
  הסיום החדש; המצב המחושב נשאר `paused` ומציג את הזמן החדש שנותר.
- עריכת טיימר פעיל משאירה אותו פעיל ומציגה את הזמן החדש שנותר.
- כרטיס האוטוליזה אינו יוצר interval פנימי כשהשעון מגיע מ־`StageScreen`.
- interval של המסך ושל `OptionalTimer` מנוקה אחרי tick שמגיע ל־`finished`.

### Verification

- בדיקות ממוקדות נכתבות ונכשלות לפני המימוש.
- הבדיקות הממוקדות, `npm test`, ‏`npm run type-check`, ‏`npm run lint` ו־RTL scan
  עוברים.

### Done when

- [x] חוזי הכשל תועדו בבדיקות לפני שינוי המימוש.
- [x] עריכת running/paused משמרת את המצב הנכון.
- [x] אין tick כפול וכל interval מסתיים ב־`finished` או ב־unmount.
- [x] 979 בדיקות, type-check, lint ו־RTL scan עוברים.

## T6 — הפעלה טבעית וחוזה press/drag אחיד

**Goal:** כל רכיב press/drag מפעיל פעולה דרך `click` טבעי, מבטל tap אחרי תנועה
ומנקה press state באופן עקבי, כך שעכבר, מגע, מקלדת וטכנולוגיות מסייעות מקבלים
אותה פעולה פעם אחת.

**Branch:** `codex/feature/26-stage-content-layers-pilot/T6-native-activation`

### Scope

- hook משותף אחד מנהל `Idle → Press → Drag/Cancel → Click → Idle` עם סף 5px
  ו־cooldown של 200ms לדיכוי click פיזי אחרי drag.
- `click` עם `detail: 0` נשאר פעיל גם בזמן cooldown כדי לא לחסום מקלדת או
  טכנולוגיה מסייעת.
- הרכיבים הפשוטים משתמשים בחוזה המשותף במקום state/refs/keydown כפולים:
  `PresetCard`, ‏`HomeCta`, ‏`ChooserCard`, ‏`StageHeader`, ‏`StarterToggle`,
  ‏`StageKnowledgeTrigger`, ‏`AutolyseCalibration` ו־`FeedbackFab`.
- `RecipeListItem` שומר את state machine של ה־swipe, אך tap עובר ל־`click`
  טבעי וה־swipe מדכא רק את ה־click הפיזי שנוצר אחריו.
- אין שינוי ב־DOM, בעיצוב, בקופי, ביעדי ניווט או בפיזיקת swipe.

### Test-first contract

- `StageHeader`, ‏`StarterToggle` ו־`RecipeListItem` מופעלים פעם אחת מ־click
  טבעי עם `detail: 0`; אין `keydown` ידני.
- tap פיזי מפעיל פעם אחת בלבד; תנועה מעל 5px, ‏`pointercancel`, ‏blur או unmount
  מנקים press state ואינם מפעילים.
- click פיזי מיד אחרי drag מדוכא; click מקלדת/AT עם `detail: 0` אינו מדוכא.
- ה־swipe של `RecipeListItem` שומר מרחק, מהירות, rubber-band ו־commit קיימים.

### Verification

- בדיקות הכשל נכשלות מול baseline של PR #82 לפני המימוש.
- בדיקות hook ורכיבים ממוקדות, הסוויטה המלאה, type-check, lint, production build
  ו־RTL scan עוברים.
- QA מרונדר אינו נדרש כשאין diff חזותי; חוזי ההתנהגות וה־DOM מכוסים בבדיקות.
- תוצאה מקומית: 95 קבצי בדיקה ו־986 בדיקות עברו.

### Done when

- [x] חוזי native click נכשלים לפני המימוש.
- [x] כל הרכיבים שב־Scope משתמשים בחוזה המשותף או ב־swipe state machine המומחה.
- [x] אין הפעלה כפולה ואין tap אחרי drag/cancel.
- [x] כל הווריפיקציות עוברות.

## T7 — מחיקת קוד מת מאומתת

**Goal:** להסיר רק קוד ללא consumer שאומת באודיט ובחיפוש עדכני, בלי refactor או
שינוי התנהגות.

**Depends on:** T6 מוזג.

### Scope

- למחוק את `components/bake/countdown-ring.tsx` אם עדיין אינו מיובא.
- להסיר את `setTimerDuration` מה־API, המימוש, ה־mocks והבדיקה אם עדיין אין caller
  בייצור.
- להסיר את `isAnalyticsReady` ואת `AnalyticsEventName` אם עדיין אין consumers.
- לפשט את `TimerStatusHeading` לטקסט ישיר אם אין השפעת layout או bidi.
- כל מועמד שנוסף לו consumer עד תחילת T7 נשאר; אין מחיקה לפי השערה.

### Verification

- חיפוש usage עדכני מתועד לפני כל מחיקה.
- בדיקות ממוקדות, הסוויטה המלאה, type-check, lint, build ו־RTL scan עוברים.
- T7 נמסר ב־PR נפרד רק אחרי מיזוג T6.

## Build Order

```text
T1–T5 merged
  ↓
T6 native activation tests fail against PR #82 baseline
  ↓
T6 shared contract + affected components
  ↓
full tests + type-check + lint + RTL scan
  ↓
one T6 PR for user review and merge
  ↓
T7 verified dead-code deletion in a separate task and PR
```
